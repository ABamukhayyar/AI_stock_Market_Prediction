# Python
venv/
__pycache__/
*.pyc

# Trained model artefacts
# Small per-symbol weights (~12 MB total) and eval metrics ARE checked in
# so teammates can clone-and-run without retraining. Only the huge
# auto-downloaded HuggingFace caches and regenerable plots are ignored.
models/finbert/
models/opus-mt-ar-en/
models/*.png
models/plots/
models/eval_v4/*.png

# Secrets / credentials
.env

# IDE / editor
.vscode/
.idea/
.ipynb_checkpoints/

# Frontend build output + deps (regenerable from package.json)
frontend/node_modules/
frontend/build/
frontend/package-lock.json

# Notebook outputs
notebooks/eda_outputs/

# Mac / Windows clutter
.DS_Store
Thumbs.db

# Daily prediction logs (written by scripts/daily_predict.bat)
logs/

# Old / unused project copies
AI_stock_Market_Prediction/
.claude/
